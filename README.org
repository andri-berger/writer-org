#+title: writer-org
#+author:
#+date:
#+language: en
#+options: toc:t num:nil ^:nil

* Overview

/One-paragraph description of what writer-org is and who it is for./

* Rationale

/Why this exists. What gap it fills. What existing tools get wrong or do not do./

* Concepts

** Annotations

/Describe what an annotation is in the context of this extension — its structure,
its lifecycle, how it differs from a comment or a note./

** Spans

/Describe what a span is: the tracked region of buffer text that an annotation
is anchored to. Explain the marker-pair model and insertion-type semantics if
relevant for users extending the package./

** Provenance

/Describe the provenance model: what metadata is attached to annotations,
where it lives (invisible text, text properties, SQLite), and why./

* Installation

** Requirements

- Emacs @@comment:version@@
- /Any external dependencies/

** From Source

#+begin_src sh
git clone https://github.com/@@comment:username@@/writer-org.git
#+end_src

Add to your ~init.el~:

#+begin_src emacs-lisp
(add-to-list 'load-path "/path/to/writer-org")
(require 'writer-org)
#+end_src

** With use-package

#+begin_src emacs-lisp
(use-package writer-org
  :load-path "path/to/writer-org"
  :config
  ())
#+end_src

* Configuration

#+begin_src emacs-lisp
;; Example configuration block — fill in variables and defaults
(setq writer-org-@@comment:option@@ @@comment:default@@)
#+end_src

| Variable | Default | Description |
|----------+---------+-------------|
|          |         |             |

* Usage

** Basic Workflow

/Describe the intended writing workflow from the user's perspective./

** Key Bindings

| Binding | Command | Description |
|---------+---------+-------------|
|         |         |             |

** Commands

*** ~writer-org-@@comment:command@@~

/Description./

* Architecture

/Optional section. Describe the internal structure for contributors or curious
users: the SQLite schema, how overlays/markers relate to the DB, the
overlay-as-authoritative-span-tracker decision, etc./

* Status

/Alpha / experimental / work in progress. What works, what is missing, what is
known to be broken./

* Contributing

/How to contribute, if at all. Code style expectations, whether issues/PRs are
welcome, etc./

* License

/License name and brief statement. Full text in ~LICENSE~./
